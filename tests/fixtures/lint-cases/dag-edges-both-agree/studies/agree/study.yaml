name: agree
baseline:
  - name: b1
    composite: pkg.composites.x
pipeline_gate:
  prerequisites:
    - {study: upstream-a, condition: tests-passed}
    - upstream-b
parent_studies:
  - upstream-a
  - {study: upstream-b, condition: ran}    # different conditions are OK — slug set matches
# Fires `dag_edges_legacy_redundant` (warning).
