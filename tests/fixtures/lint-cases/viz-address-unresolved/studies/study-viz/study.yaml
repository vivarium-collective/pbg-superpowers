name: study-viz
baseline:
  - name: b1
    composite: pkg.composites.x
visualizations:
  - name: dnaa-state
    address: "local:DnaAStateVisualization"   # class does not exist anywhere under pkg/visualizations/
  - name: box-occupancy
    address: "local:DnaABoxOccupancyVisualization"
  - name: ts-from-obs
    address: "pbg_superpowers.visualizations.TimeSeriesFromObservables"   # dotted path — linter does not check
  - name: empty-addr
    address: ""                                                            # empty — linter skips
  - name: bare-name
    address: "TimeSeriesPlot"                                              # no local: prefix — linter skips
