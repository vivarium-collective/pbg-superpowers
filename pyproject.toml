[project]
name = "pbg-superpowers"
version = "0.14.0"
description = "Plugin Python helpers for the pbg-superpowers skills"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "ruamel.yaml>=0.18",
    "jsonschema[format-nongpl]>=4.21",
    "jinja2>=3.1",
    "click>=8.1",
    "bigraph-schema>=0.0.60",
    "process-bigraph>=0.0.66",
    "pypdf>=4.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.4", "pytest-cov>=4.1", "pytest-timeout>=2.1"]
evaluator = ["pbg-emitters[parquet]"]

[tool.uv.sources]
pbg-emitters = { git = "https://github.com/vivarium-collective/pbg-emitters.git", branch = "main" }

[project.scripts]
pbg-scaffold = "pbg_superpowers.scaffold:cli"
pbg-feedback-import = "pbg_superpowers.feedback_import:cli"
pbg-migrate-nested = "pbg_superpowers.migrate_nested:main"
pbg-migrate-inputs = "pbg_superpowers.migrate_inputs:main"
pbg-backfill-runs = "pbg_superpowers.backfill_runs:main"
pbg-compute-outcomes = "pbg_superpowers.study_evaluator:compute_outcomes_cli"
pbg-sync-runs = "pbg_superpowers.study_outcomes:main"
pbg-migrate-readouts = "pbg_superpowers.readout_migration:main"
pbg-populate-simulation-set = "pbg_superpowers.simulation_set:main"
pbg-populate-findings = "pbg_superpowers.finding_observations:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pbg_superpowers"]
# templates/ and server/ live outside the package but are required at runtime
# (report renderer, scaffold helper, server scripts). Force-include them in the
# wheel so non-editable installs still work.
[tool.hatch.build.targets.wheel.force-include]
"templates" = "pbg_superpowers/_templates"
"server" = "pbg_superpowers/_server"

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["tests/fixtures"]
