# pbg_{{ model_slug }}

Composite for: {{ model_name }}

## Layout
- `pbg_{{ model_slug }}/core.py` — `build_core()` registers processes + types
- `pbg_{{ model_slug }}/wiring.py` — connection table
- `pbg_{{ model_slug }}/document.py` — `build_document()` produces the Composite spec
- `tests/` — assembly, adapters, baseline run, core integration
- `phases/` — multi-phase plan + per-phase deliverables
- `reports/index.html` — interactive deep-dive (regenerated by /pbg-report)
