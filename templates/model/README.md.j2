# viva_{{ model_slug }}

<!-- BEGIN:dashboard -->
<!-- `vivarium-workbench gen-readme` fills this with a prominent link to the
     published read-only dashboard (derived from the git remote). Run it after
     pushing to GitHub; the publish-dashboard workflow keeps the dashboard live. -->
<!-- END:dashboard -->

Composite for: {{ model_name }}

## Layout
- `viva_{{ model_slug }}/core.py` — `build_core()` registers processes + types
- `viva_{{ model_slug }}/wiring.py` — connection table
- `viva_{{ model_slug }}/document.py` — `build_document()` produces the Composite spec
- `tests/` — assembly, adapters, baseline run, core integration
- `reports/index.html` — interactive deep-dive (regenerated by /viva-report)
