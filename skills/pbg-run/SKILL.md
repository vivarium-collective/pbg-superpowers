---
name: pbg-run
description: DEPRECATED ALIAS for /viva-run (pbg→viva rebrand). Use /viva-run.
user-invocable: true
allowed-tools: Bash(*) Read
argument-hint: <composite-id> [--steps N] [--emit p1,p2,...]
---

# pbg-run  →  renamed to `/viva-run`

`/pbg-run` was renamed **`/viva-run`** in the pbg→viva rebrand. This alias
stays functional during the deprecation window (removed in a future major).

**Use [`/viva-run`](../viva-run/SKILL.md)** — read that skill and follow it.
All guidance lives there; nothing is maintained in two places.
