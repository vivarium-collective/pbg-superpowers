---
name: pbg-status
description: DEPRECATED ALIAS for /viva-status (pbg→viva rebrand). Use /viva-status.
user-invocable: true
allowed-tools: Bash(*) Read
argument-hint: (no args)
---

# pbg-status  →  renamed to `/viva-status`

`/pbg-status` was renamed **`/viva-status`** in the pbg→viva rebrand. This alias
stays functional during the deprecation window (removed in a future major).

**Use [`/viva-status`](../viva-status/SKILL.md)** — read that skill and follow it.
All guidance lives there; nothing is maintained in two places.
